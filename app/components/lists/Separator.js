import React from 'react';
import { View } from 'react-native';

function Separator() {
    return (
        <View style={{width: "100%", height: 0.2, backgroundColor: "#ddd"}}/>
    );
}

export default Separator;